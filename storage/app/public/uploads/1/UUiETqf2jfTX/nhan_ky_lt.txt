what is Json?
1. là định dạng dữ liệu(chuỗi)
2.Json -> JavaScript Object Notation
3. data type : Number, String, boolean, Null, Array, Object

// Stringify: Từ javascript types -> Json
//Parse: Từ Json -> Javascript

what is promise? // là một khái niệm thao tác bất động bồ trước khi có promise ta có callback hell nó sẽ bị sâu vào và rất khó nhìn, rễ bị rối, thằng promise được sinh ra để thay thế callback hell, promise sẽ gọn gàng dễ hiểu hơn ko bị rối
1. Sync
2. Async

//Promise sinh ra để xử lý các thao tác bất đồng bộ.
- Trước khi có Promise, ngta thường dùng Callback => dễ gây ra Callback hell => sinh ra Promise từ ES6.
- Promise có 3 trạng thái: Pending (memory leak), Fulfilled, Rejected
- Để tạo ra 1 Promise, sử dụng từ khóa new.
- Trong constructor của Promise, truyền vào 1 executor function.
- Trong executor function nhận 2 tham số (resolve, reject). Resolve(): xử lý thành công >< Reject(): xử lý thất bại.
- Promise có 2 phương thức chính: .then và .catch (đều nhận callback).